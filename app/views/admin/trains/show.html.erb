<p id="notice"><%= notice %></p>

<p><td><tr>
  <strong>ID:</strong>
   <%= @train.id %></tr>

  <strong>Номер:</strong>
  <%= @train.number %></tr></td></p>
  <p>
  <strong>На станции:</strong>
  <% if @train.current_station.present? %>
  <p><%= @train.current_station.title %></p>
  <% else %>
  <h3>Станция не указана</h3>
  <% end %>


<%= link_to 'Редактировать', edit_admin_train_path(@train) %> |
<%= link_to 'Назад к списку', admin_trains_path %>
